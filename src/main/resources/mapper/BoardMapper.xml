<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.boardapi.board.mapper.board.BoardMapper" >
    <resultMap id="BaseResultMap" type="Board">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
    </resultMap>

    <sql id="columns">
        ${alias}id AS ${prefix}id
        , ${alias}title AS ${prefix}title
        , ${alias}content AS ${prefix}content
        , ${alias}create_date AS ${prefix}create_date
        , ${alias}update_date AS ${prefix}update_date
    </sql>

    <insert id="insert" parameterType="Board">
        INSERT INTO board
        (id
        , title
        , content
        ) VALUES
            (
             #{id}
            ,#{title}
            ,#{content}
            )
    </insert>

    <select id="search" parameterType="Board" resultMap="BaseResultMap">
        SELECT
        <include refid="columns">
            <property name="alias" value=""/>
            <property name="prefix" value=""/>
        </include>
        FROM board
        <if test=" null != type ">
            <if test=' type == "title"'>
                 WHERE title LIKE CONCAT(#{keyword}, '%')
            </if>
            <if test=' type == "content"'>
                WHERE content LIKE CONCAT(#{keyword}, '%')
            </if>
            <if test=' type == ""'>
                WHERE content LIKE CONCAT(#{keyword}, '%')
                     OR title LIKE CONCAT(#{keyword}, '%')
            </if>
        </if>
    </select>

    <select id="fetch" resultMap="BaseResultMap" parameterType="Board">
        SELECT
        <include refid="columns">
            <property name="alias" value=""/>
            <property name="prefix" value=""/>
        </include>
          FROM board
         WHERE id = #{id}
    </select>

    <update id="update" parameterType="Board">
        UPDATE board
           SET title = #{title}
             , content = #{content}
             , update_date = current_timestamp()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Board">
        DELETE FROM board
              WHERE id = #{id}
    </delete>
</mapper>